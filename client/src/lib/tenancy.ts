/**
 * Client-side single-tenant constant.
 *
 * This app is single-tenant. Company ID 1 is the only company.
 * Use this instead of hardcoded `?? 1` fallbacks.
 */
export const DEFAULT_COMPANY_ID = 1;
